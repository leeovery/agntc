AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to specification and project conventions. All spec decision points are correctly implemented: parser disambiguation via prefix detection, constraint validation with semver.validRange(), manifest storage as optional field with absence as the signal, tag normalization pipeline using clean() not coerce(), version resolution via semver.maxSatisfying(), bare-add default ^X.Y.Z derivation, explicit constraint resolution, exact-tag and branch-ref producing no constraint, re-add overwriting constraint correctly, change-version stripping constraint, update routing based on constraint presence, out-of-constraint collation at end of update output with info tone, list display using constraint arrow ref format, collection constraint propagation, and the never-downgrade guard via isAtOrAboveVersion. The semver package is correctly placed in production dependencies.
