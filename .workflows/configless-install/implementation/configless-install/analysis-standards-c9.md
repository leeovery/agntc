AGENT: standards
CYCLE: 9
STATUS: clean
FINDINGS_COUNT: 0

SUMMARY: Implementation conforms to the configless-install specification and project (TypeScript) conventions. Full spec-conformance pass over detection (type-detection.ts), config model (config.ts), agent selection (agent-select.ts), collection pipeline (add.ts / collection-select.ts), manifest type recording + legacy backfill (manifest.ts), derive-before-delete replay (nuke-reinstall-pipeline.ts / clone-reinstall.ts), and copy-safety guards (copy-safety.ts). Verified: single structural detection path; precedence `--plugin` > config `type` > structure; only `"plugin"` recognised (all other type values incl. `"collection"` ignored); type-vs-structure conflicts hard-error with correct attribution; skills-only ambiguous case defaults to collection with one-level enumeration of `skills/<name>` (member identity keyed by basename, segment locates the dir), overridable by `type: plugin` / `--plugin`; collection membership structural one-level-down via qualifiesAsMember, nested collections skipped, per-member configless agent default, skills-only child forced to plugin only when it has its own asset dirs; agent-selection KNOWN_AGENTS fallback for the three no-valid-constraint cases, auto-select scoped to declared-single-agent; ManifestEntry.type optional with in-memory files-based backfill (anti-drift), single-skill ambiguity → skill; derive-before-delete per recorded type before any nuke, abort/blocked/no-agents leave install intact with distinct messaging; symlink guard on add + update, path-traversal pre-read, lexical relative()-based containment. No spec drift or MUST-DO/MUST-NOT violations found. (NOTE: standards examined the detection/keying for spec conformance of the install path; the architecture agent's update-round-trip finding for skills-only members is an architecture/lifecycle concern surfaced separately in c9.)
