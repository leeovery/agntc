AGENT: standards
CYCLE: 11
STATUS: clean
FINDINGS_COUNT: 0

SUMMARY: Implementation conforms to the configless-install specification and project TypeScript conventions. Both cycle-10 findings are remediated: `resolveAgents` (src/nuke-reinstall-pipeline.ts:270) now treats a defined-but-empty `configAgents` array identically to `undefined` (lenient no-restriction, matching the add path and the spec's unified "no valid constraint → lenient default" rule), and `cloneAndReinstall` (src/clone-reinstall.ts:373-386) now applies `assertSubpathWithinClone` to the stored `sourceSubpath` before the join, mirroring add's step-2c path-traversal guard. Spot-checks confirmed: structural type detection precedence (--plugin > config type > structure) with the type-vs-structure hard-error path; recorded-type replay with derive-before-delete (skill→SKILL.md present, plugin→≥1 asset dir present) and intact-install abort; legacy `type` backfill from local `files` on manifest read; structural collection membership one level down with skills-only inner-member relocation via `sourceSubpath`; basename manifest keying unchanged; tagless→HEAD (`ref: null`) reuse; symlink-escape pre-flight on both add and update bounded at the clone root with lexical broken-link semantics; agntc.json stripped from bare-skill destinations; auto-select scoped to declared-single-agent only with KNOWN_AGENTS fallback and detected pre-tick in the configless default; change-version strips constraint. Typecheck clean (tsc --noEmit) and all 1519 tests pass.
