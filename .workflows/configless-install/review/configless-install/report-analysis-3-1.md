TASK: configless-install-analysis-3-1 — Give update's symlink-escape its own copy-safety/blocked outcome+message (not the derive-before-delete "aborted"/remove+add channel). Severity medium.

ACCEPTANCE CRITERIA: escaping symlink on update → copy-safety/blocked message describing the escape, NOT "type no longer supported"/remove+add; install intact (no nuke/copy, manifest entry unchanged); buildAbortMessage/aborted exercised only for genuine recorded-type mismatches; add and update describe the same violation with consistent copy-safety framing.

STATUS: Complete

SPEC CONTEXT: Symlink guard boundary/broken links/update coverage (385-399) — pre-flight scan before any copy/nuke ("validate before you mutate"), identical guard for add + update re-copy, error before writing, install intact on violation, boundary=clone root. Spec frames symlink-escape as copy-safety/security boundary (412), structurally distinct from a recorded-type change. Spec doesn't prescribe exact wording → task's framing-consistency governs.

IMPLEMENTATION: Implemented (all 5 Do steps landed).
- New CloneReinstallBlocked {status:"blocked";reason} + union member (src/clone-reinstall.ts:228-251); CloneReinstallAborted reserved for derive-before-delete (222-226).
- Pipeline NukeReinstallBlocked variant + symlink-escape catch returns it (NOT aborted) (nuke-reinstall-pipeline.ts:64-84,103-109); runPipeline re-emits blocked (clone-reinstall.ts:454-459).
- buildCopySafetyMessage beside buildAbortMessage (282-287): "${key}: ${reason}. The source contains a symlink escaping the clone, so the update is blocked. The existing install is unchanged." — mirrors add path identity-prefix, no remove+add remedy.
- Dispatch: CloneReinstallFailure union (111-115), isCloneReinstallFailure (140-149), mapCloneFailure onBlocked on status alone (158-160), failureMessage blocked→buildCopySafetyMessage (195), CloneFailureHandlers.onBlocked (122-123). Consumers: update.ts single-key onBlocked → p.log.error(buildCopySafetyMessage)+ExitSignal(1) (234-242); all-updates onBlocked → PluginOutcome{status:"blocked"} summary (334-343); union member (50); summary renders blocked as error (589-594); hasFailedOutcome counts blocked (621-628); manifest-build loop only adds updated/refreshed removes copy-failed → blocked entries intact. List actions inherit via failureMessage.
- Add path unchanged (identity-prefixed cancel, 344-357). buildAbortMessage now reached only for true recorded-type mismatches (replayRecordedSkill missing SKILL.md 181-188; replayRecordedPlugin no asset dir 220-227).

TESTS: Adequate. clone-reinstall.test.ts: update escaping symlink → status:"blocked" reason names link, no nuke/copy (891-920); entry NOT removed, distinct from copy-failed (922-948); dispatch via onBlocked (950-985); buildCopySafetyMessage names key+reason+blocked+unchanged, NOT "no longer supports that type"/"npx agntc remove"/"To migrate", mentions "symlink", NOT equal buildAbortMessage (1296-1328); consistency — update message starts with add-path identity-prefix + "points outside the clone" (1330-1341). Recorded-type mismatch still routes aborted/buildAbortMessage + remove/add (395-442; update.test.ts:1223). Integration: single-key symlink-escape non-zero + copy-safety message + install intact (1384-1418); all-updates blocked non-zero entry preserved sibling updates (1420-1497). Regression: add-path symlink-escape cancels identity-prefixed install intact (add.test.ts:6408); collection member (6605). Negative-wording at list-action level too. 1:1 to all four ACs.

CODE QUALITY: Conventions followed (status single cross-boundary discriminator like aborted precedent; exhaustive mapCloneFailure; union co-located; builders decoupled from presentation). SOLID good (blocked clean open/closed extension single-site; classification/message/presentation separated). Complexity low (guard-clause pre-flight at top, linear dispatch). Modern idioms (exhaustive narrowing → compile error on new status). Readability good (doc comments state install-intact + not-copy-failed + why no remove+add; consumer comments "same posture as onAborted but DIFFERENT message").

BLOCKING ISSUES: None.

NON-BLOCKING NOTES:
- [do-now] src/clone-reinstall.ts:126-133 — orphaned + now-stale JSDoc. Documents mapCloneFailure but sits above isCloneReinstallFailure (which has its own comment 134-139); mapCloneFailure (151) has no leading doc. WORSE: this task makes its content wrong — it describes status-dispatched cases as "aborted (derive-before-delete / symlink escape, install intact) and no-agents", conflating symlink-escape with aborted, the exact conflation this task removed. Delete the orphan; fold an accurate summary into mapCloneFailure's own doc listing aborted (derive-before-delete), blocked (symlink-escape copy-safety), no-agents (lenient skip). (RECURRING: also flagged by analysis-1-6 and analysis-2-4 reviews; this task adds a correctness reason.)
