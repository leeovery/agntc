AGENT: architecture
FINDINGS:
- FINDING: Cross-surface "recovery" regression tests mock the very function whose fix they claim to verify
  SEVERITY: low
  FILES: tests/update-check-all.test.ts:208-222, tests/commands/update.test.ts:384-409, tests/commands/update.test.ts:759-776, tests/commands/list-detail.test.ts:499-520
  DESCRIPTION: The three tests explicitly labelled as v4-branch "cross-surface recovery" guards all stub out or bypass the real classifier. update-check-all's "surfaces a real status ... for a v4-branch entry" sets mockCheckForUpdate.mockResolvedValue({ status: "up-to-date" }); update's single-key and all-plugins v4 tests do the same; list-detail passes updateStatus directly. Because checkForUpdate is mocked to return up-to-date, every one of these tests would pass byte-for-byte against the PRE-FIX code (which returned check-failed for a real v4 probe) — the mock, not the classification logic, produces the "recovered" status. They document intent and pin surface behaviour given a good status, but they do not guard the regression: nothing exercises the real classifyAndCheck output flowing into a consuming surface. The only true regression guard for the fix is the unit coverage in tests/update-check.test.ts (child_process mocked). The end-to-end seam (v4 branch entry -> real ls-remote probe -> classifier -> surface) is verified only as the composition of two independently-mocked halves, never in one flow. This matches the codebase's established test architecture (surfaces unit-test against a mocked checkForUpdate), so it is not a coverage hole so much as a mislabelled-confidence risk: the "regression guard" naming overstates what these assertions protect.
  RECOMMENDATION: Keep the surface tests as-is for surface behaviour, but treat tests/update-check.test.ts:163-198 (the v4 branch classification cases with realistic ls-remote payloads) as the canonical regression guard and reference it from the surface tests' comments, or rename the surface tests to reflect that they assert status-driven rendering rather than "recovery." No new test machinery required; this is a labelling/expectation correction so a future reader does not mistake the mocked pass-throughs for end-to-end protection.
- FINDING: The classification probe embeds git plumbing inline in update-check.ts instead of a git-utils helper paralleling fetchRemoteTagRefs
  SEVERITY: low
  FILES: src/update-check.ts:126-136, src/update-check.ts:50-75, src/git-utils.ts:49-54
  DESCRIPTION: update-check.ts now hosts three remote-lookup sites at two different abstraction levels: the tag/constrained paths delegate to the git-utils helper fetchRemoteTagRefs (which owns arg construction, the 15s timeout, and parseTagRefs), while both checkHead's HEAD lookup and the new classifyAndCheck probe build their ls-remote arg arrays inline, pass the timeout inline, and parse locally (parseLsRemoteSha / parseRefProbe). The new probe therefore leaks git-command knowledge into the module that should own comparison logic, and parseRefProbe — a general "sha\trefpath" scanner conceptually sibling to parseTagRefs — lives in update-check.ts exported purely for its unit test, while the analogous parseLsRemoteSha next to it stays private. This is the carried-over half of the c1 parser-scatter observation (the c1 asymmetric-comparison half is now resolved by the shared compareResolvedSha helper). The spec explicitly permitted either home for the parser, and checkHead's inline pattern predates this fix, so this is sanctioned and low-risk; it is a cohesion/seam-placement nit, not a latent bug.
  RECOMMENDATION: When this module is next touched, consider a fetchRemoteRefProbe(url, ref): { headSha, tagSha } helper in git-utils.ts alongside fetchRemoteTagRefs, moving parseRefProbe with it, so all ls-remote plumbing and output parsing sit in one module and update-check.ts is left with pure classification/comparison. Alternatively, at minimum treat parseRefProbe and parseLsRemoteSha with consistent visibility. Not required for correctness.
SUMMARY: The bugfix is architecturally sound and tightly scoped: the change is confined to checkForUpdate, the UpdateCheckResult union is untouched, the tiebreak-by-tag routing is clean, the branch arm correctly reuses the probed head sha (no extra round-trip), and the c1 duplicated-comparison finding is resolved via the shared compareResolvedSha helper. Two low-severity observations remain — the v4 "cross-surface recovery" tests mock checkForUpdate and so pin surface behaviour rather than guard the fix end-to-end, and the probe embeds ls-remote plumbing inline in update-check.ts rather than in a git-utils helper (carried from c1, spec-sanctioned). Neither introduces a latent bug or fragile assumption.
