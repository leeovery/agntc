AGENT: duplication
FINDINGS:
- FINDING: ls-remote single-line tokenisation re-authored in parseRefProbe
  SEVERITY: low
  FILES: src/update-check.ts:64-66, src/update-check.ts:39-41
  DESCRIPTION: parseRefProbe (new) tokenises an ls-remote line with `const parts = line.split("\t"); const sha = parts[0]?.trim() ?? ""; const refPath = parts[1]?.trim() ?? "";`. parseLsRemoteSha (same file) performs the same `sha\trefpath` split to pull the first field (`firstLine.split("\t")[0]!.trim()`). The same primitive is also hand-rolled in the pre-existing parseTagRefs (git-utils.ts, out of scope). The spec correctly sanctioned a *separate classifier* — the existing parsers cannot classify a mixed heads+tags response — but the low-level "split one `sha\trefpath` line into sha + refPath" step did not need to be re-expressed to gain that. Two in-scope parsers now each open-code the tab split and trim. This was flagged in cycle 1 and remains unaddressed.
  RECOMMENDATION: Factor the single-line split into one shared primitive (e.g. `splitRefLine(line): { sha: string; refPath: string }`) and have parseRefProbe and parseLsRemoteSha consume it; the classifier's exact-path matching and parseLsRemoteSha's first-line-only selection stay where they are, only the tokenisation is consolidated. Low urgency — it is a one-to-two-line idiom.
- FINDING: catch-to-check-failed translation repeated across the four check functions
  SEVERITY: low
  FILES: src/update-check.ts:132-134, src/update-check.ts:165-169, src/update-check.ts:186-190, src/update-check.ts:236-240
  DESCRIPTION: The new classifyAndCheck carries a fourth copy of the module's `catch (err: unknown) { return { status: "check-failed", reason: (err as Error).message }; }` block, matching checkHead, checkTag, and checkConstrained. Each independently narrows the unknown error and maps it onto the check-failed variant, so the reason-extraction rule (`(err as Error).message`) is now stated in four places and any change to how errors are surfaced needs four edits. The new copy is at least consistent with the established convention (three of the four sites pre-date this fix), so extraction would touch pre-existing bodies beyond the bugfix's tight scope. Flagged in cycle 1 and still open.
  RECOMMENDATION: Extract `toCheckFailed(err: unknown): UpdateCheckResult` returning `{ status: "check-failed", reason: (err as Error).message }` and reuse it at all four catch sites, removing the drift risk in one move. Lowest priority — the block is small and idiomatic — and it is optional given it would reach into pre-existing functions.
SUMMARY: This is a tightly scoped bugfix and duplication is minor; cycle 1's resolved-sha-comparison finding has since been consolidated into the shared compareResolvedSha helper (now used by both classifyAndCheck and checkHead), so only two low-severity candidates remain — the re-authored ls-remote line tokenisation and the four-times-repeated catch-to-check-failed mapping. The test additions (buildRefProbeOutput, the args-routing mockLsRemote, and the v4-branch cross-surface tests spanning checkAllForUpdates, runUpdate, and renderDetailView) are cleanly factored, reuse existing helpers, and exercise genuinely distinct surfaces — no test-side duplication worth extracting.
