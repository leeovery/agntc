AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to the specification and project conventions. The bugfix reshapes `checkForUpdate`'s dispatch exactly as specified — steps 1-3 (local / constrained / HEAD) are unchanged, and the lexical `isTagRef` split (steps 4-5) is replaced by `classifyAndCheck`, which issues the single `ls-remote refs/heads/{ref} refs/tags/{ref}` probe at the mandated `{ timeout: 15_000 }`. The new `parseRefProbe` matches on exact ref paths, is order-independent, and ignores the peeled `^{}` line (all covered by dedicated unit tests). The both-present tiebreak resolves to the tag (routing on `tagSha` first), the branch path reuses the probed head sha with no second lookup (asserted by an execFile-called-once test), the tag path re-issues its own `--tags` (asserted by the probe-then-tags sequence test), and the neither-found case returns `check-failed` with the exact unified reason `Ref '{ref}' not found on remote as a branch or tag`. `isTagRef` and the old `checkBranch` are fully removed with no dangling references; the per-type "not found" strings are retired and no stale assertion remains. Scope is respected: `UpdateCheckResult` union unchanged, no manifest/add-side change, no new dependencies, constrained/HEAD/local paths untouched. All seven acceptance criteria and all six new-regression test cases are present, plus cross-surface coverage in update-check-all, update, and list-detail. Error handling (`catch (err: unknown)` → `(err as Error).message`) and exported-helper-for-testing patterns match the existing conventions of the file and codebase (`parseTagRefs`, `hasOutOfConstraintVersion`). New tests use exact `.toEqual` assertions where output is deterministic, honoring the code-quality anti-pattern on substring assertions.
