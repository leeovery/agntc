AGENT: duplication
FINDINGS:
- FINDING: Remote-sha-vs-installed-commit comparison duplicated between classifyAndCheck and checkHead
  SEVERITY: low
  FILES: src/update-check.ts:129-134, src/update-check.ts:154-157
  DESCRIPTION: The new branch path in classifyAndCheck reproduces, verbatim, the "is the remote sha == the installed commit?" decision that already lives in checkHead. Both are the identical three-line shape — `if (sha === installedCommit) return { status: "up-to-date" }; return { status: "update-available", remoteCommit: sha };` — differing only in the local variable name (headSha vs remoteSha). This is the exact logic the removed checkBranch used to hold; folding checkBranch into classifyAndCheck carried the comparison across a task boundary rather than sharing it. It is the single "am I current?" rule for a resolved sha, now authored in two places that must stay in lockstep (e.g. if the union ever gains a field on update-available).
  RECOMMENDATION: Extract a tiny pure helper, e.g. `compareResolvedSha(remoteSha: string, installedCommit: string): UpdateCheckResult` returning up-to-date / update-available, and call it from both classifyAndCheck (branch path) and checkHead. Keeps the two current-vs-behind decisions derived from one definition. Low urgency — three lines — but it is the cleanest genuine consolidation here.

- FINDING: ls-remote line-parse primitive re-authored in parseRefProbe
  SEVERITY: low
  FILES: src/update-check.ts:64-66, src/update-check.ts:39-41, tests/helpers/git-mocks.ts (parse counterpart in git-utils.ts parseTagRefs)
  DESCRIPTION: parseRefProbe (new) splits an ls-remote line into sha + ref-path with `const parts = line.split("\t"); const sha = parts[0]?.trim() ?? ""; const refPath = parts[1]?.trim() ?? "";`. That per-line extraction is byte-for-byte the same as the pre-existing parseTagRefs (git-utils.ts) and the same first-field logic as parseLsRemoteSha in this very file. The spec deliberately sanctioned a *separate classifier* (existing parsers can't classify a mixed heads+tags response), and that separation is correct — but the low-level "split a `sha\trefpath` line" step did not need to be duplicated to achieve it. Three parsers now each hand-roll the tab split and trim.
  RECOMMENDATION: Factor the single-line split into one shared primitive (e.g. `splitRefLine(line): { sha: string; refPath: string }`, natural home git-utils.ts alongside parseTagRefs) and have parseRefProbe, parseTagRefs, and parseLsRemoteSha consume it. The classifier's exact-path matching and the tag stripping stay where they are; only the shared tokenisation is consolidated.

- FINDING: catch-to-check-failed translation repeated a fourth time
  SEVERITY: low
  FILES: src/update-check.ts:119-121, src/update-check.ts:158-163, src/update-check.ts:179-184, src/update-check.ts:229-234
  DESCRIPTION: classifyAndCheck adds a fourth copy of the module's `catch (err: unknown) { return { status: "check-failed", reason: (err as Error).message }; }` block, matching checkHead, checkTag, and checkConstrained. Each independently narrows the unknown error and maps it onto the check-failed variant. The new copy is consistent with the established convention (which is good), but the reason-extraction rule (`(err as Error).message`) is now stated in four places; a change to how errors are surfaced would need four edits.
  RECOMMENDATION: Extract `toCheckFailed(err: unknown): UpdateCheckResult` (returning `{ status: "check-failed", reason: (err as Error).message }`) and reuse it at all four catch sites. Lowest priority of the three — the block is small and idiomatic — but it is the clearest copy-paste-across-boundaries instance and consolidating it removes the drift risk in one move.

SUMMARY: This is a tightly scoped bugfix and duplication is minor. Three low-severity consolidation candidates emerged as the new remote-truth classifier (classifyAndCheck / parseRefProbe) re-expressed logic that already existed elsewhere in update-check.ts / git-utils.ts: the resolved-sha comparison, the ls-remote line tokenisation, and the catch-to-check-failed mapping. Each is a small, honest extraction; none is high-impact. The test additions (buildRefProbeOutput, the args-routing mockLsRemote, the v4-branch cross-surface tests) are cleanly factored and reuse existing helpers appropriately — no test-side duplication worth extracting.
