AGENT: standards
FINDINGS:
- FINDING: checkHead body refactored despite spec listing it as "untouched"
  SEVERITY: low
  FILES: src/update-check.ts:83-94, src/update-check.ts:152-171
  DESCRIPTION: The spec scopes the change to `checkForUpdate` ("Dispatch change (confined to checkForUpdate)") and explicitly lists `checkHead` as an untouched non-goal ("checkHead untouched — HEAD-tracking entries unaffected"). The implementation extracted a shared `compareResolvedSha` helper and rewrote `checkHead`'s tail to call it (`return compareResolvedSha(remoteSha, installedCommit)`), so the branch arm of `classifyAndCheck` and `checkHead` now share one comparison routine. The refactor is behaviour-preserving (identical up-to-date / update-available logic) and aligns with code-quality's "Compose, Don't Duplicate," so HEAD-tracking behaviour is genuinely unaffected — but it is a literal deviation from the "confined to checkForUpdate" scope statement, and it couples the HEAD path to a helper now co-owned by the branch path. Worth confirming the shared-helper extraction was an intended scope choice rather than incidental drift.
  RECOMMENDATION: Confirm the `compareResolvedSha` extraction was a deliberate, accepted scope extension. If strict adherence to the "checkHead untouched" non-goal is preferred, keep `compareResolvedSha` used only by the branch arm and leave `checkHead`'s inline comparison as-is. No behavioural change is required either way.
SUMMARY: The remote-truth classifier is faithfully implemented — single heads+tags `ls-remote` probe at the 15s sibling timeout, exact-path order-independent `parseRefProbe`, tag-wins tiebreak, unified "not found as a branch or tag" terminal failure, branch-sha reuse (no second lookup), tag path re-issuing its own `--tags`, `isTagRef`/`checkBranch` fully removed, and all seven acceptance criteria plus cross-surface recovery covered by tests. The only drift is a benign, behaviour-preserving refactor of `checkHead`, a function the spec flagged as untouched.
