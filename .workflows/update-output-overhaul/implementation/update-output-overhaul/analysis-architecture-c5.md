AGENT: architecture
FINDINGS: none
SUMMARY: Implementation architecture is sound — clean one-directional layering (update-check → update-groups → update-render → commands/update), single-sourced seams (grouping key, PluginOutcome constructors, GroupTargetFacets projection, resolveGuardedSourceDir containment guard, groupLabel, the once-computed divergent flag), and genuine composition (out-of-constraint derived from latestOverall !== null on both surfaces, categorizeMember pure/shared across singleton and grouped paths, formatVersionMove/newestTag single-sourced). The grouped path has end-to-end command-level coverage (clone-once, one-check-per-group, per-group persistence, clone-failure collapse-vs-enumerate). The only items weighed — OutOfConstraintInfo's dual key?/label? display slot and the inline move-shape vs VersionMoveInput (deliberately narrower non-null oldCommit) — are low-severity working design or out-of-scope duplication, not material architectural concerns.
