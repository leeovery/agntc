AGENT: standards
FINDINGS:
- FINDING: Version-move arrow renders ASCII "->" while agent separators and the spec's illustrated output use unicode "→" (persisting from c1, unresolved)
  SEVERITY: low
  FILES: src/version-resolve.ts:59,62, src/update-render.ts:161,164,233, src/summary.ts:243,291
  DESCRIPTION: `formatVersionMove` emits `<old> -> <new>` (ASCII arrow, comment pins it "verbatim"), but `formatMemberLine` renders the member/agent separator with unicode `→` (`${name} → ${agents}`), and `formatGroupHeader` interpolates the ASCII move into an otherwise-unicode line. A single divergent-old member line therefore mixes both glyphs — `macos → claude  (v1.2.0 -> v1.3.0)` — whereas every illustrative shape in the spec (lines 144, 148, 161, 218) renders the move with `→`. The substantive Part-3 rule (tags only when both refs are semver tags AND the ref moved) is implemented correctly; this is purely the arrow glyph and the within-line inconsistency. The ASCII arrow is plausibly a deliberate preservation of the pre-existing `6500f65 -> f395397` format (Overview line 9), and the spec itself mixes "from…to"/"→"/"->", so the char is not a ratified decision — hence low. Because `formatVersionMove` is the single shared source for both the reworded summary surface and the new grouped surface, unifying the glyph is a one-site change but also touches single-key summary wording.
  RECOMMENDATION: Pick one arrow glyph for user-facing moves — switch `formatVersionMove` to `→` to match the separator and the spec's shown output, or knowingly accept ASCII as house style. If changed, update the version-resolve doc comment pinning " -> " as "verbatim". Safe to defer.

- FINDING: Group-of-one no-agents skip loses the specified ⚠ glyph in the collapsed spinner stop-frame (persisting from c1, unresolved)
  SEVERITY: low
  FILES: src/commands/update.ts:724-726, src/commands/update.ts:801-811
  DESCRIPTION: Spec line 175 says a group-of-one "collapses its single member's outcome into the header line, carrying the same ✓/✗/⚠ per its result," and line 173 specifies `no-agents` renders as `⚠ member: skipped …` (`p.log.warn`). In `streamGroupWork`, a single-updating-member group emits its outcome as the spinner stop-frame via `spin.stop(line.text, line.level === "error" ? 2 : 0)`. clack's `spinner.stop` exposes only success (0) and error (2) codes — no warn — so a group-of-one whose reinstall returns `no-agents` (level "warn" from `collapsedMemberLine`→`failureOrSkipMemberLine`) stops with code 0 and shows the neutral glyph rather than ⚠. The text still carries "skipped — no longer supports installed agents", so the signal is not lost, and the multi-member path renders ⚠ correctly via `p.log.warn`; only the group-of-one collapse of a no-agents result deviates. aborted/blocked (error → ✗) map fine; only the warn level lacks a stop code. This is an accepted clack-API limitation already acknowledged in the code comment.
  RECOMMENDATION: If exact glyph parity with spec line 175 matters, render a group-of-one no-agents skip as a separate `p.log.warn` line (stopping the spinner on the bare header) instead of as the stop-frame, so ⚠ is preserved. Otherwise record it as an accepted clack limitation in the spec's working notes.

- FINDING: Several new all-mode helpers use positional 4-parameter signatures against the codebase's object-param convention (persisting from c2, deferred)
  SEVERITY: low
  FILES: src/update-render.ts:74 (formatNewerTagsLine — four positional strings), src/update-groups.ts:310 (reinstallMember), src/update-groups.ts:384 (processGroupUpdate), src/commands/update.ts:687 (streamGroupWork), src/commands/update.ts:855 (streamGroupMemberLines)
  DESCRIPTION: code-quality.md lists "Long parameter lists (4+)" as an anti-pattern. Several new functions take exactly four positional parameters while the surrounding pipeline code deliberately bundles inputs into a single object (`PipelineInput`, `cloneRepoOnce`'s `{key,entry,newRef}`, `formatGroupHeader`'s input object, `VersionMoveInput`, `MemberLineInput`). The highest-risk instance is `formatNewerTagsLine(label, commandTarget, pinnedRef, newestTag)` — four same-typed strings, so a caller could transpose arguments without a type error. The others mix heterogeneous types (and bundle `facets`/`item`), so the practical risk is lower and each is well-documented. No behavioural or spec impact. c2 assessed this as safe to defer.
  RECOMMENDATION: Optional. If aligning with the object-param convention, wrap the four same-typed-string `formatNewerTagsLine` in a named input object (as `formatGroupHeader` already does) to eliminate positional-transposition risk. Safe to defer — not worth churning working, tested code for.
SUMMARY: Independent from-scratch review confirms the implementation conforms to the specification across all ten acceptance criteria — clone dedup grouping by `(resolvedCloneUrl, versionIntent = constraint ?? ref)` with `ref` excluded for constrained groups (singly-updated members stay grouped), one resolve + one clone per group, per-member categorization against the shared target, intended genuine-state splits, per-group manifest persistence before each ✓ streams, clone-failure fan-out to N `failed` outcomes rendered as one enumerated grouped line, per-member reinstall isolation with the per-member `sourceSubpath` containment guard preserved via `resolveGuardedSourceDir`, tag-vs-hash wording (both refs semver tags AND ref moved) on both single-key and all-mode surfaces, the actionable mode-matched out-of-constraint footer (post-bump current, per-group collapse, bare re-add), the all-mode `newer-tags` add-command consistency fix, and the ratified single-key-1-vs-all-mode-0 exit posture. No new drift and no missing validations found; the only observations are three persisting low-severity presentation/style nits already surfaced in earlier cycles.
