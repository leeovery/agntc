AGENT: standards
FINDINGS:
- FINDING: New all-mode helpers use positional 4-parameter signatures against the codebase's own object-param convention
  SEVERITY: low
  FILES: src/update-groups.ts:273 (reinstallMember), src/update-groups.ts:359 (processGroupUpdate), src/update-groups.ts:123 (mapReinstallResultToOutcome), src/commands/update.ts:698 (streamGroupWork), src/commands/update.ts:869 (streamGroupMemberLines), src/commands/update.ts:903 (emitMemberLine), src/update-render.ts:74 (formatNewerTagsLine)
  DESCRIPTION: code-quality.md lists "Long parameter lists (4+)" as an anti-pattern. Several new functions in this feature take exactly four positional parameters, while the surrounding pipeline code deliberately bundles inputs into a single object (PipelineInput/runPipeline, cloneRepoOnce's `{key, entry, newRef}`, formatGroupHeader's input object, VersionMoveInput). The new grouped/streaming helpers diverge from that established convention. This is genuinely borderline — objects like `facets` and `item` already bundle several values, so the effective input count is higher than four and the signatures read clearly with their JSDoc — so it is a mild internal-consistency nit, not a spec drift. No behavioural impact.
  RECOMMENDATION: Optional. If aligning with the codebase's object-param convention, wrap the hottest of these (e.g. reinstallMember, streamGroupWork) in a single named input object as runPipeline/formatGroupHeader already do. Safe to defer — not worth churning working, well-documented code for.
SUMMARY: Implementation conforms to the specification across all ten acceptance criteria — clone dedup grouping by `(resolvedCloneUrl, versionIntent)` with ref excluded for constrained groups, one resolve/clone per group, per-member categorization against the shared target, genuine-state splits, per-group manifest persistence before streaming, clone-failure fan-out to N `failed` outcomes with one grouped display line, per-member reinstall isolation with the sourceSubpath containment guard preserved, tag-vs-hash wording on both surfaces, mode-matched actionable out-of-constraint/newer-tags collapse, and the ratified single-key-vs-all-mode exit posture. Typecheck is clean and all 1710 tests pass. The only observation is a low-severity parameter-list convention nit; no spec-conformance drift found.
