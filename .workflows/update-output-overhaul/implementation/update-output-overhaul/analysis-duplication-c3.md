AGENT: duplication
FINDINGS:
- FINDING: Parallel OutOfConstraintInfo builders across the single-key and all-mode surfaces
  SEVERITY: medium
  FILES: src/commands/update.ts:116-145 (extractOutOfConstraint), src/commands/update.ts:520-539 (groupOutOfConstraintInfo)
  DESCRIPTION: Two functions independently construct the same `OutOfConstraintInfo` record from two sources — the single-key path from an `UpdateCheckResult`, the all-mode path from a `GroupTarget`. Both encode the identical field-assembly shape (`current`, `latestOverall`, `repo`, `constraint`, plus a key/label discriminator) AND the same "post-bump current" business rule (the footer must name the tag this run landed on, not the pre-bump `entry.ref`). That rule is expressed twice with divergent mechanics: `extractOutOfConstraint` computes `checkResult.status === "constrained-update-available" ? checkResult.tag : entry.ref!`, while `groupOutOfConstraintInfo` always uses `target.tag`. The out-of-constraint guard is likewise duplicated in two forms (`hasOutOfConstraintVersion(checkResult) && entry.constraint !== undefined` vs `target.kind !== "constrained" || target.latestOverall === null`). The spec (Blocking message → "Names the post-bump current version") explicitly requires this current-version wording to stay consistent across both surfaces, so a fix applied to one builder that misses the other silently reintroduces the stale-current bug the spec calls out. The near-verbatim "POST-BUMP current" comment block appears at both sites, confirming the copy.
  RECOMMENDATION: Extract one `buildOutOfConstraintInfo` core in summary.ts (next to the `OutOfConstraintInfo` type) that takes the already-resolved primitives — `current` (post-bump tag), `latestOverall`, `repo`, `constraint`, and the key-or-label — and returns the record once. Each caller keeps only its source-specific extraction (mapping its check-result/target into those primitives + the null guard); the field assembly and the post-bump-current contract then live in exactly one place and cannot drift between surfaces.

- FINDING: Never-downgrade constrained guard duplicated across single-key and grouped categorization
  SEVERITY: medium
  FILES: src/commands/update.ts:190-194 (runSingleUpdate), src/commands/update.ts:558-563 (splitMember)
  DESCRIPTION: Both call sites re-apply the same post-categorization rule after a `constrained-update-available` verdict: `if (isAtOrAboveVersion(entry.ref, result.tag)) → treat as up-to-date, do not reinstall`. `runSingleUpdate` demotes to the "already up to date" outro; `splitMember` (the new all-mode group-first path) demotes to `upToDateOutcome(key)` and skips pushing the member into `updating`. This is a correctness-bearing guard (never downgrade a member already at/above the resolved tag), and `splitMember`'s own comment points back to "update.ts's isAtOrAboveVersion" — i.e. the all-mode build re-authored the single-key guard inline across the task boundary rather than sharing it. `categorizeMember` (update-check.ts) already returns `constrained-update-available` without this check, so every consumer must remember to re-apply it; nothing enforces that a future third consumer (or a change to the rule) stays in step.
  RECOMMENDATION: Fold the never-downgrade demotion into the single categorization authority — either inside `categorizeMember` (compare `entry.ref` against `target.tag` and return `constrained-up-to-date` when at/above) or via a shared `isConstrainedDowngrade(entry, tag)` predicate co-located with `isAtOrAboveVersion` — so both surfaces consume one rule and the guard cannot be forgotten at a call site.

- FINDING: Triplicated "already up to date" outro-and-return block in runSingleUpdate
  SEVERITY: low
  FILES: src/commands/update.ts:157-160, src/commands/update.ts:178-181, src/commands/update.ts:191-194
  DESCRIPTION: The exact two-line block `p.outro(\`${key} is already up to date.\`); return { newEntry: null, outOfConstraint };` is written three times — for `up-to-date`, `constrained-up-to-date`, and the `constrained-update-available` never-downgrade branch. A Rule-of-Three trigger: the sentinel result literal and the outro wording are pinned in three spots, so a wording tweak or a change to the `SingleUpdateResult` no-op shape must touch all three.
  RECOMMENDATION: Introduce a small local `alreadyUpToDate()` helper (or a `const noChange = { newEntry: null, outOfConstraint }` plus one outro) and return it from the three arms. Proportionality note: each block is only two lines, so this is a low-impact tidy — worth doing alongside the finding above (both cluster in runSingleUpdate's status switch), not on its own.
SUMMARY: The feature's earlier consolidation passes already resolved the c2 findings (the sourceSubpath guard, the `failedOutcome` factory, and the `isSuccessOutcome` guard all now exist and are shared). What remains is lower-grade parallelism between the single-key and all-mode surfaces: two independent `OutOfConstraintInfo` builders that duplicate the post-bump-current rule, the never-downgrade constrained guard re-authored in both categorization sites, and a triplicated up-to-date outro block. All three are consolidations of existing logic, not new behaviour.
