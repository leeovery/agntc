AGENT: duplication
FINDINGS:
- FINDING: sourceSubpath containment guard + source-dir resolution duplicated across the singleton and grouped reinstall paths
  SEVERITY: high
  FILES: src/clone-reinstall.ts:389-404, src/update-groups.ts:290-310
  DESCRIPTION: The per-member path-traversal pre-flight — `if (entry.sourceSubpath) { try { assertSubpathWithinClone(tempDir, entry.sourceSubpath) } catch (err) { if (err instanceof PathTraversalError) return <clone-failed>; throw err } }` immediately followed by `const sourceDir = resolveUpdateSourceDir(tempDir, key, entry.sourceSubpath)` — is byte-for-byte identical in both files except for how the failure is shaped (clone-reinstall returns a raw `CloneReinstallResult`; update-groups maps it through `mapReinstallResultToOutcome`). The guard in `cloneAndReinstall` pre-dates this feature; the new group orchestrator's `reinstallMember` (task 1-4) re-authored it inline rather than sharing it. This is a security-sensitive guard (symlink/`../` escape rejection) now living in two copies across the task boundary the seam split — exactly the copy-paste-drift-across-boundaries case: a future fix to the escape rule (or a change to `resolveUpdateSourceDir`'s pairing with the guard) must be applied in two places or the two clone entry points silently diverge on what counts as an escape. The code comments in both sites even acknowledge one is "mirrored" from the other.
  RECOMMENDATION: Extract a single guarded-resolution helper (e.g. `resolveGuardedSourceDir(cloneRoot, key, sourceSubpath): { ok: true; sourceDir } | { ok: false; message }`) that runs `assertSubpathWithinClone` and then `resolveUpdateSourceDir`, returning the result-object shape already used elsewhere in this codebase (`PrepareReinstallResult`). Place it next to `resolveUpdateSourceDir` in source-parser.ts (its natural home — that function is already the shared source-dir authority both callers use). Each call site keeps only its own one-line failure mapping (raw `clone-failed` result vs `mapReinstallResultToOutcome`); the guard logic and the `PathTraversalError`-vs-rethrow discrimination live in exactly one place.

- FINDING: `failed` PluginOutcome construction repeated six times
  SEVERITY: medium
  FILES: src/commands/update.ts:326, src/commands/update.ts:340, src/update-groups.ts:150, src/update-groups.ts:155, src/update-groups.ts:327, src/update-groups.ts:384
  DESCRIPTION: The object literal `{ status: "failed", key, summary: \`${key}: Failed — ${msg}\` }` is hand-built at six sites across two files: the `prepareReinstall`-not-ok branch and the outer catch in `processUpdateForAll`; the `onCloneFailed` and `onUnknown` arms of `mapReinstallResultToOutcome`; the `reinstallMember` catch; and the clone-fatal fan-out `map` in `processGroupUpdate`. Every copy encodes the same three facts — the discriminant `"failed"`, the key, and the `"<key>: Failed — <message>"` summary wording. The wording and the outcome shape must stay in lockstep across all six, but nothing enforces it (one already reads `member.key` instead of `key`, an easy place for the prefix to drift from the message body).
  RECOMMENDATION: Add a `failedOutcome(key: string, message: string): PluginOutcome` factory in update-groups.ts (where `PluginOutcome` is defined) that returns the literal once, and call it from all six sites. Collapses the "<key>: Failed — …" phrasing to a single source and makes a new failure origin a one-line call.

- FINDING: success-outcome predicate (`updated | refreshed`) re-inlined across five sites
  SEVERITY: low
  FILES: src/commands/update.ts:813, src/commands/update.ts:855, src/commands/update.ts:914, src/commands/update.ts:968, src/commands/update.ts:1040
  DESCRIPTION: The check `outcome.status === "updated" || outcome.status === "refreshed"` — "is this a persisted-success outcome" — is written inline in five functions (`collapsedMemberLine`, `streamCollapsedOutcome`, `emitMemberLine`, `persistUnitOutcomes`, `renderOutcomeSummary`), several of which then reach into `outcome.newEntry` on the true branch. The two-status success set is thus encoded in five places; adding a future success variant (or renaming one) requires finding and editing all five, and the `newEntry`-access sites repeat the same widen-then-narrow with no shared type guard.
  RECOMMENDATION: Extract a named type guard `isSuccessOutcome(outcome): outcome is Extract<PluginOutcome, { status: "updated" | "refreshed" }>` (co-located with `PluginOutcome` in update-groups.ts) and use it at all five sites. Names the intent, narrows to `newEntry` for the sites that need it, and puts the success-status set in one place. (Proportional note: each site is one line, so this is a low-impact consolidation — worth doing while the file is open, not on its own.)
SUMMARY: One high-severity duplicate remains — the security-sensitive sourceSubpath containment guard re-authored inline in the new group orchestrator instead of shared with the singleton clone path — plus a repeated `failed`-outcome literal (6x) and an inlined success-status predicate (5x) that are straightforward helper extractions. The feature's earlier consolidation pass (tasks 5-2/5-3/5-4) already removed the GroupTarget-projection, member-line, and key/tag helper duplications; pre-existing overlaps (formatLabel/formatRefLabel, GitHub-URL derivation) were confirmed out of scope.
